from socket import *
from time import sleep
for i in range(10):
  s = socket(AF_INET, SOCK_STREAM)
  s.connect(('127.0.0.1', 5400))
  s.send(b"Hello, world!")
  print(s.recv(1024))
  s.send(b"end")
  s.close()
  sleep(1)